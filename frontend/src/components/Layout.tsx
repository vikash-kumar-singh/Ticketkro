import { BarChart3, Bell, ChevronLeft, LayoutDashboard, LogOut, Menu, Moon, PlusCircle, Settings, TicketCheck, Users, X } from 'lucide-react'; import { useState } from 'react'; import { NavLink, Outlet } from 'react-router-dom'; import { useAuth } from '../store/auth';
const links=[{to:'/',label:'Overview',icon:LayoutDashboard},{to:'/tickets',label:'Tickets',icon:TicketCheck},{to:'/tickets/new',label:'Raise ticket',icon:PlusCircle},{to:'/users',label:'User management',icon:Users,admin:true},{to:'/reports',label:'Reports',icon:BarChart3,staff:true}];
export function Layout(){const [open,setOpen]=useState(false);const {user,logout}=useAuth();const staff=user?.role!=='employee';const admin=user?.role==='super_admin'||user?.role==='company_admin';return <div className="min-h-screen bg-slate-50"><aside className={`fixed inset-y-0 left-0 z-40 w-64 bg-ink text-white transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex h-20 items-center justify-between px-6"><div><div className="text-xl font-bold">Resolve<span className="text-indigo-400">.</span></div><div className="text-[10px] uppercase tracking-[.2em] text-slate-400">Service desk</div></div><button className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><nav className="space-y-1 px-3">{links.filter(l=>(!l.staff||staff)&&(!l.admin||admin)).map(({to,label,icon:Icon})=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 text-sm ${isActive?'bg-indigo-500 text-white':'text-slate-300 hover:bg-white/5'}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="absolute bottom-4 left-3 right-3"><button onClick={logout} className="flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm text-slate-300 hover:bg-white/5"><LogOut size={18}/>Sign out</button></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-slate-200 bg-white/90 px-4 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><div><p className="text-sm text-slate-500">Welcome back,</p><h1 className="font-semibold">{user?.name}</h1></div></div><div className="flex items-center gap-2"><button className="rounded-xl p-2.5 hover:bg-slate-100"><Moon size={19}/></button><button className="relative rounded-xl p-2.5 hover:bg-slate-100"><Bell size={19}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-rose-500"/></button><div className="ml-2 hidden text-right sm:block"><p className="text-sm font-semibold">{user?.name}</p><p className="text-xs capitalize text-slate-500">{user?.role.replace('_',' ')}</p></div></div></header><main className="p-4 md:p-8"><Outlet/></main></div>{open&&<button aria-label="Close navigation" className="fixed inset-0 z-30 bg-black/30 lg:hidden" onClick={()=>setOpen(false)}/>}</div>}
